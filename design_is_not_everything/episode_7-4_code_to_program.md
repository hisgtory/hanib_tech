# Episode 7-4. 코드 100줄이 프로그램 1만 줄이 되는 비밀

개발자가 작성한 간단한 코드 100줄이 어떻게 거대한 프로그램이 될까요? 그 비밀을 알아봅시다.

## 🧩 라이브러리와 프레임워크의 마법

개발자가 작성하는 코드는 빙산의 일각입니다:

```python
import requests
response = requests.get('https://api.github.com')
print(response.json())
```

이 3줄의 코드 뒤에는:
- HTTP 프로토콜 구현 코드 수천 줄
- JSON 파싱 라이브러리 코드 수백 줄  
- 네트워크 통신 라이브러리 코드 수천 줄

<callout>
🏗️ **건물 건축으로 비유하면**
개발자는 설계도만 그리고, 실제 건축은 미리 만들어진 부품(라이브러리)들을 조립해서 완성합니다.
</callout>

## 📦 의존성의 눈덩이 효과

하나의 라이브러리가 또 다른 라이브러리들을 필요로 합니다:

```
내 코드 (100줄)
└── Express.js (웹 프레임워크)
    ├── Body-parser (요청 파싱)
    ├── Cookie-parser (쿠키 처리)  
    └── Static (정적 파일 서빙)
        ├── Send (파일 전송)
        └── Mime (파일 타입 감지)
```

## 🎯 컴파일러의 최적화

컴파일러는 코드를 최적화하면서 크기를 늘립니다:
- **인라인 함수**: 자주 쓰는 함수를 직접 삽입
- **루프 전개**: 반복문을 펼쳐서 속도 향상
- **에러 처리**: 각종 예외 상황 대응 코드 추가

<hl>개발자는 "무엇을" 만들지 정의하고, 컴퓨터가 "어떻게" 만들지 결정합니다!</hl>
-----

## 📝 Feedback

### 현재 잘된 점
- 핵심 개념을 명확하게 설명
- 실무 예시와 사례 제공
- 단계별 가이드 포함

### 개선이 필요한 부분

1. **디자이너 관점 추가**
   - 디자이너가 이해해야 할 핵심 포인트
   - 디자인 작업과의 연관성
   - 협업 시 주의사항

2. **실무 대화 예시**
   - 팀 미팅에서의 실제 대화
   - 갈등 상황과 해결 방법
   - 효과적인 커뮤니케이션 팁

3. **실패 사례와 교훈**
   - 실제 프로젝트 실패 경험
   - 피해야 할 함정들
   - 배운 교훈과 개선 방안

4. **실용적 도구/템플릿**
   - 바로 사용 가능한 템플릿
   - 추천 도구와 서비스
   - 체크리스트와 가이드라인

5. **시각적 요소 강화**
   - 프로세스 다이어그램
   - 비교 표와 차트
   - 인포그래픽 추가 제안
